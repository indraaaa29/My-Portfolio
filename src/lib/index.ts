/**
 * Library utilities barrel.
 *
 * Reusable utility functions used across the application.
 */
export { cn, formatNumber, clamp, mapRange, debounce, throttle } from './utils';
export { lockScroll, unlockScroll, registerLenis } from './scroll-lock';
